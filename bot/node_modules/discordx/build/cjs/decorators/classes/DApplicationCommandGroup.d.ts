import { Decorator } from "@discordx/internal";
import type { DApplicationCommand, DApplicationCommandOption } from "../index.js";
declare type CreateStructure<T> = {
    name: string;
    payload: Partial<T>;
    root?: string;
};
/**
 * @category Decorator
 */
export declare class DApplicationCommandGroup<InfoType = DApplicationCommand | DApplicationCommandOption> extends Decorator {
    name: string;
    root?: string;
    payload: Partial<InfoType>;
    protected constructor(options: CreateStructure<InfoType>);
    static create<T = DApplicationCommand | DApplicationCommandOption>(options: CreateStructure<T>): DApplicationCommandGroup<T>;
}
export {};
