import type { GuildChannel, GuildMember, Role, ThreadChannel, User } from "discord.js";
import type { SimpleCommandMessage } from "../../index.js";
import type { IGuild, IPrefix, NotEmpty } from "../index.js";
export declare type ArgSplitter = string | RegExp | ((command: SimpleCommandMessage) => string[]);
export declare type SimpleCommandOptions<T extends string = string> = {
    aliases?: string[];
    argSplitter?: ArgSplitter;
    botIds?: string[];
    description?: string;
    directMessage?: boolean;
    guilds?: IGuild[];
    name?: NotEmpty<T>;
    prefix?: IPrefix;
};
export declare type SimpleCommandOptionOptions<T extends string = string> = {
    description?: string;
    name: NotEmpty<T>;
    type: SimpleCommandOptionType;
};
export declare type SimpleOptionType = string | number | boolean | ThreadChannel | GuildChannel | User | GuildMember | Role | undefined;
export declare enum SimpleCommandOptionType {
    String = 0,
    Number = 1,
    Boolean = 2,
    User = 3,
    Channel = 4,
    Role = 5,
    Mentionable = 6
}
