import { Decorator } from "@discordx/internal";
/**
 * @category Decorator
 */
export class DApplicationCommandGroup extends Decorator {
    name;
    root;
    payload;
    constructor(options) {
        super();
        this.name = options.name;
        this.root = options.root;
        this.payload = options.payload ?? {};
    }
    static create(options) {
        return new DApplicationCommandGroup(options);
    }
}
//# sourceMappingURL=DApplicationCommandGroup.js.map